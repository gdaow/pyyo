 * Put fields in a Meta subclass to avoid linter getting annoyed by shadowed
   fields.
 * Add a default value for fields.
 * Add a required flag for fields.
 * Add a tag to load an object / sequence from another file.
 * Add a tag to specify object type.
 * Add LICENSE & README.
 * Start documentation.